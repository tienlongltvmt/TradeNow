/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import ButtomNavigation from './src/navigation/ButtomNavigation';

AppRegistry.registerComponent(appName, () => ButtomNavigation);
